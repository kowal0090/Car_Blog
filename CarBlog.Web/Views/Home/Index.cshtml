@model IEnumerable<Post>

@{ 
    ViewBag.Title = "Blog o samochodach";
    ViewBag.Description = "Blog samochodowy";
    ViewBag.Keywords = "Salon";
}

@*@<h2>Home Page</h2>
<img src="~/content/static/jpg1.jpg" />*@

<div class="main-img">
    <span class="title">
        Blog o samochodach <br /> Projekt Aplikacje WWW <br />
        <span class="authors">Marcin Kowacki</span>
    </span>
    
</div>

<div class="container">
    @*<ul>
        @foreach (var post in Model)
        {
            <li>
                <a asp-controller="home" asp-action="post" asp-route-id="@post.Id">
                    @post.Title
                </a>
            </li>
        }
    </ul>*@

    @foreach (var post in Model)
    {
        var image_path = $"/Image/{post.Image}";
    <a class="post" asp-controller="home" asp-action="post" asp-route-id="@post.Id">
        <img src="@image_path" width="1000" />
        <span class="title">
            @post.Title
        </span>

    </a>
    }

</div>